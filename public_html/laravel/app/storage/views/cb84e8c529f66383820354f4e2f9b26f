$.each($('.sort_link'), function(index, element){

	$(element).closest('th').css('cursor', 'pointer');
	$(element).closest('th').on('click', function(e){

		$(e.target).find(':eq(0)').click();

	});

});